diff --git a/nematus/translate.py b/nematus/translate.py
index e39eec6..ad5b955 100755
--- a/nematus/translate.py
+++ b/nematus/translate.py
@@ -18,6 +18,8 @@ from compat import fill_options
 from hypgraph import HypGraphRenderer
 from settings import TranslationSettings
 
+from timeit import default_timer as timer
+
 class Translation(object):
     #TODO move to separate file?
     """
@@ -322,6 +324,8 @@ class Translator(object):
 
         # logging
         logging.debug('{0} - {1}\n'.format(process_id, idx))
+        if idx == 0:
+            logging.info("Time start for process {}: {}".format(process_id, timer()))
 
         # sample given an input sequence and obtain scores
         sample, score, word_probs, alignment, hyp_graph = self._sample(input_item, trng, fs_init, fs_next, gen_sample)
@@ -476,6 +480,8 @@ class Translator(object):
                                           target_probs=word_probs,
                                           hyp_graph=hyp_graph)
                 translations.append(translation)
+
+        logging.info('Time end for all processes: {}'.format(timer()))
         return translations
 
     def translate_file(self, input_object, translation_settings):
